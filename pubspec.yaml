name: flutter_helpers
version: 0.0.1

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=3.19.6"

dependencies:
  flutter:
    sdk: flutter

  material_ui: ^1.1.0
  intl: ^0.20.3

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true